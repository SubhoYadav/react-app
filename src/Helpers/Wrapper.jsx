const Wrapper = (props) => {
  return props.children;
};

export default Wrapper;

// This component serves as a wrapper for multiple adjacent elements that we wish to return from our react component
